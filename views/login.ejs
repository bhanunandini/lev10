<!DOCTYPE html>
<html lang="en">
  <head>
  
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width,initial-scale=1.0" />
    <meta name="csrf-token" content= "<%= csrfToken %>">
    <link href="./css/style.css" rel="stylesheet" />
    <title>
        <%= title %>
    </title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/style.css" rel="stylesheet" />
  </head>
  
  <body style="background-color: rgba(206, 234, 234, 0.925);">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
          <%- include("header.ejs") %> <br>
          <a href="/" class="bg-green-600 text-white px-5 py-2 rounded font-medium mr-2 mb-2 border-gray-300"><-Go Back To Home Page</a>
          <h6 class="py-4">Login with your account</h6>
          <form action="/session" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="py-3">
                <label for="email">Email:-</label>
                <input type="email" name="email" class="border border-red-300 text-red-900 rounded p-2 w-full" required>
            </div>  
            <div class="py-3"> 
                <label for="password">Password:</label>
                <input type="password" name="password" class="border border-red-300 text-red-900 rounded p-2 w-full" required>
            </div>  
            <div class="py-3"> 
                <button type="submit" class="text-white bg-blue-600 hover:bg-blue-1000 font-medium rounded px-5 py-2">Sign In Now</button>        
            </div>  
          </form>

          <div class="flash">
            <% if(messages && messages.error && messages.error.length > 0)  { %>
              <% for(var i=0; i<messages.error.length; i++) { %>
                      <li> <%= messages.error[i] %></li>
              <% } %>
            <% } %>
          </div>
          
        </div>
    </div>
  </body>
</html>